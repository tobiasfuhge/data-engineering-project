apiVersion: argoproj.io/v1alpha1
kind: Sensor
metadata:
  name: minio-train-sensor
  namespace: argo-events
spec:
  template:
    serviceAccountName: sa-read-data-storage-argo-events
  dependencies:
    - name: minio-dep
      eventSourceName: minio-input-eventsource
      eventName: input               
      filters:
        data:
          - path: notification.0.s3.object.key
            type: string
            value:
              - ".*\\.csv$"
            comparator: "="        

  triggers:
    - template:
        name: trigger-workflow
        k8s:
          group: argoproj.io
          version: v1alpha1
          resource: workflows
          namespace: argo-workflows
          operation: create
          source:
            resource:
              apiVersion: argoproj.io/v1alpha1
              kind: Workflow
              metadata:
                generateName: v5-final-parallel-ml-pipeline-
                namespace: argo-workflows
              spec:
                entrypoint: main
                arguments:
                  parameters:
                    - name: filename
                      value: ""
                workflowTemplateRef:
                  name: v5-final-parallel-ml-pipeline-cncs4
          parameters:
            - src:
                dependencyName: minio-dep
                dataKey: notification.0.s3.object.key
              dest: spec.arguments.parameters.0.value