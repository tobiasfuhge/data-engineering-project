apiVersion: argoproj.io/v1alpha1
kind: Sensor
metadata:
  name: minio-train-sensor
  namespace: argo-events
spec:
  template:
    serviceAccountName: argo-events-sa
  dependencies:
    - name: minio-dep
      eventSourceName: minio-input-eventsource
      eventName: input               
      filters:
        data:
          - path: notification.0.s3.object.key
            type: string
            value:
              - ".*\\.csv$"       

  triggers:
    - template:
        name: trigger-workflow
        k8s:
          group: argoproj.io
          version: v1alpha1
          resource: workflows
          namespace: argo-workflows
          operation: create
          source:
            resource:
              apiVersion: argoproj.io/v1alpha1
              kind: Workflow
              metadata:
                generateName: v8-final-parallel-ml-pipeline-
                namespace: argo-workflows
              spec:
                entrypoint: main
                arguments:
                  parameters:
                    - name: filename
                      value: ""
                workflowTemplateRef:
                  name: v8-final-parallel-ml-pipeline-qkhh9 
          parameters:
            - src:
                dependencyName: minio-dep
                dataKey: notification.0.s3.object.key
              dest: spec.arguments.parameters.0.value