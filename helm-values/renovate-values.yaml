renovate:
  config: |
    {
      "platform": "github",
      "token": "${GITHUB_TOKEN}",
      "repositories": ["your-org/your-repo"]
    }

extraEnv:
  - name: GITHUB_TOKEN
    valueFrom:
      secretKeyRef:
        name: renovate-github-token
        key: token
