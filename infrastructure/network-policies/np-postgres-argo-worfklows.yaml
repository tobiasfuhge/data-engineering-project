apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-argo-workflows-to-postgres
  namespace: data-storage
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: postgres
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: argo-workflows
    ports:
    - protocol: TCP
      port: 5432