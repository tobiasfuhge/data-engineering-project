apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: airflow-statsd-monitor
  namespace: airflow
  labels:
    release: prometheus-stack 
spec:
  selector:
    matchLabels:
      component: statsd
  endpoints:
  - port: statsd-metrics
    interval: 30s
    path: /metrics